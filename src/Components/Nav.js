import React from 'react'

const Nav = () => {
  return(
    <nav>
      <h1>Reaction Gallery</h1>
      <button>Randomize</button>
      <button>Favorites</button>
      <button>Home</button>
    </nav>
  )
}

export default Nav
